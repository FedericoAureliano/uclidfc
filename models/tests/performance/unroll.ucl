// UCLID TEST
// MaxParseTime=0.5
// MaxProcessTime=0.5
// MaxGenerationTime=0.5

module main {
    var x : integer;

    init {
        x = 0;
    }

    next {
        x = x + 1;
    }

    invariant failat10: x < 10;

    control {
        unroll(500);
    }
}