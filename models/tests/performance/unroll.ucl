// UCLID TEST
// MaxParseTime=0.5
// MaxProcessTime=1.5
// MaxGenerationTime=0.5

module main {
    var x : integer;

    init {
        x = 0;
    }

    next {
        assert(x > 0);
        x = x + 1;
        assert(x > 0);
    }

    invariant failat10: x < 10;

    control {
        unroll(500);
    }
}