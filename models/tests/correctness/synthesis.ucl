// UCLID TEST
// Solver=cvc4
// Result=Some(false)

module main {
  synthesis function h(x : integer, y : integer) : boolean;
  var a, b : integer;

  init {
    a = 0;
    b = 1;
  }
  next {
    a', b' = b, a + b;
  }

  invariant a_le_b: a <= b && h(a, b);

    control
    {
        induction;
        check;
    }
}