// UCLID TEST
// Solver=z3
// Result=None

module main {
    var x : integer;
    var y : boolean;

    next {
        havoc y;
        x = x + 1;
    }

    control {
        trace(5); // fuzzing is tracing without a starting point: generates a random starting point
        print_cex(x, y);
    }
}