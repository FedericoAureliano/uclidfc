// UCLID TEST
// Solver=z3
// Result=None

module main {
    var x : integer;
    var y : boolean;

    init {
        x = 0;
    }

    next {
        havoc y;
        x = x + 1;
    }

    control {
        trace(main(7, false), 5, false);
        print_cex(x, y);
    }
}