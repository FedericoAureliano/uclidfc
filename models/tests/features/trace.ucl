// UCLID TEST
// Solver=z3
// Result=None

module main {
    var x : integer;
    var y : boolean;

    init {
        x = 0;
    }

    next {
        havoc y;
        x = x + 1;
    }

    control {
        trace(5, false, {
            x = 7;
            y = false;
        });
        print_cex(x, y);
    }
}