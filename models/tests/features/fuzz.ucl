// UCLID TEST
// Solver=z3
// Result=None

type some_enum = enum {A, B, C};
type userfun;

module main {
    var x : integer;
    var y : boolean;
    var z : some_enum;
    var w : userfun;

    next {
        havoc y;
        x = x + 1;
    }

    control {
        trace(5); // fuzzing is tracing without a starting point: generates a random starting point
        print_cex(x, y);
    }
}